diff --git a/apps/web/src/lib/auth/identities.ts b/apps/web/src/lib/auth/identities.ts
--- a/apps/web/src/lib/auth/identities.ts
+++ b/apps/web/src/lib/auth/identities.ts
@@ -12,3 +12,3 @@
 export function isOAuthProvider(value: unknown): value is OAuthProvider {
-  return value === "google" || value === "github";
+  return value === "google" || value === "github" || value === "email";
 }
