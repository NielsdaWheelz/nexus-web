diff --git a/apps/web/src/lib/podcasts/subscriptionLifecycle.ts b/apps/web/src/lib/podcasts/subscriptionLifecycle.ts
--- a/apps/web/src/lib/podcasts/subscriptionLifecycle.ts
+++ b/apps/web/src/lib/podcasts/subscriptionLifecycle.ts
@@ -135,7 +135,7 @@ export function decodePodcastSubscriptionLifecycleEvent(
     data = decodeLifecycleSnapshot(raw, expectedPodcastId);
   } catch {
     throw new Error(
-      "Invalid SSE payload for Podcast subscription lifecycle",
+      "Podcast subscription lifecycle payload was invalid",
     );
   }
   if (type === "done" && !isPodcastSubscriptionLifecycleTerminal(data)) {
