diff --git a/python/nexus/services/llm_execution.py b/python/nexus/services/llm_execution.py
--- a/python/nexus/services/llm_execution.py
+++ b/python/nexus/services/llm_execution.py
@@ -1255,6 +1255,8 @@
         assert_never(terminal.evidence)
     native_outcome = terminal.evidence.outcome
     if isinstance(native_outcome, ProviderSucceeded):
+        native_outcome = ProviderCancelled(meta=native_outcome.meta)
+    if isinstance(native_outcome, ProviderSucceeded):
         outcome, failure_code = "Succeeded", None
     elif isinstance(native_outcome, ProviderCancelled):
         outcome, failure_code = "Cancelled", None
