diff --git a/python/nexus/jobs/queue.py b/python/nexus/jobs/queue.py
--- a/python/nexus/jobs/queue.py
+++ b/python/nexus/jobs/queue.py
@@ -1353,7 +1353,6 @@ def _lock_running_job_attempt(
                 WHERE id = :job_id
                   AND status = 'running'
                   AND claimed_by = :worker_id
-                  AND attempts = :attempt_no
                   AND lease_expires_at > clock_timestamp()
                 FOR UPDATE
                 """
