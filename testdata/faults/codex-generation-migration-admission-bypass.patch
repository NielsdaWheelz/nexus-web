diff --git a/migrations/alembic/versions/0224_codex_personal_generation.py b/migrations/alembic/versions/0224_codex_personal_generation.py
--- a/migrations/alembic/versions/0224_codex_personal_generation.py
+++ b/migrations/alembic/versions/0224_codex_personal_generation.py
@@ -261,6 +261,6 @@ def _preflight(bind: sa.Connection) -> None:
               AND NOT (
                   status = 'dead'
-                  AND kind = ANY(CAST(:resettable_dead_kinds AS text[]))
+                  AND kind = ANY(CAST(:kinds AS text[]))
               )
             ORDER BY id
             """
