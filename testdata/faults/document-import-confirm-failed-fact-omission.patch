diff --git a/python/nexus/services/media_upload_sessions.py b/python/nexus/services/media_upload_sessions.py
--- a/python/nexus/services/media_upload_sessions.py
+++ b/python/nexus/services/media_upload_sessions.py
@@ -1134,15 +1134,9 @@
             error_code=exc.code.value,
         )
         raise
-    except Exception:
-        logger.info(
-            "ConfirmFailed",
-            upload_session_id=str(session_id),
-            generation=generation,
-            request_id=request_id,
-            error_code="E_INTERNAL",
-        )
-        raise
+    # Fault: a publication-transaction or commit failure settles the confirm with
+    # no keyed ConfirmFailed fact, leaving operators with ConfirmStarted and
+    # silence.
 
     finalize_upload_session_storage_object_write(
         db,
