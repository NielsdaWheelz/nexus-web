diff --git a/python/nexus/services/rate_limit.py b/python/nexus/services/rate_limit.py
--- a/python/nexus/services/rate_limit.py
+++ b/python/nexus/services/rate_limit.py
@@ -441,7 +441,7 @@ class RateLimiter:
                     usage_date=usage_date,
                     charged_tokens=normalized_tokens,
                 )
-            if not existing_charge and normalized_tokens > 0:
+            if normalized_tokens > 0:
                 db.execute(
                     text(
                         """
