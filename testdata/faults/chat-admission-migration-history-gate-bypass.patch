diff --git a/migrations/alembic/versions/0226_chat_admission_receipts.py b/migrations/alembic/versions/0226_chat_admission_receipts.py
--- a/migrations/alembic/versions/0226_chat_admission_receipts.py
+++ b/migrations/alembic/versions/0226_chat_admission_receipts.py
@@ -26,9 +26,5 @@ def upgrade() -> None:
         sa.text("LOCK TABLE chat_runs, background_jobs IN EXCLUSIVE MODE")
     )
-    if connection.scalar(sa.text("SELECT EXISTS(SELECT 1 FROM chat_runs)")):
-        raise RuntimeError(
-            "pre-receipt chat history exists; stop writers and audit the hard-cut maintenance gate"
-        )
     if connection.scalar(
         sa.text("SELECT EXISTS(SELECT 1 FROM background_jobs WHERE status='running')")
     ):
