diff --git a/apps/web/src/components/reader/ReaderDocumentMapDetail.tsx b/apps/web/src/components/reader/ReaderDocumentMapDetail.tsx
--- a/apps/web/src/components/reader/ReaderDocumentMapDetail.tsx
+++ b/apps/web/src/components/reader/ReaderDocumentMapDetail.tsx
@@ -43,7 +43,7 @@
     ? { kind: "Section", sectionId: currentSection.value.section.section_id }
     : { kind: "Document" });
   const selectedSection = selection.kind === "Section"
-    ? structure.sections.find((entry) => entry.section.section_id === selection.sectionId)
+    ? (currentSection.kind === "Present" ? currentSection.value : undefined)
     : undefined;
   if (selection.kind === "Section" && !selectedSection) {
     throw new Error("Selected map section is absent from the current source.");
