diff --git a/migrations/alembic/versions/0218_podcast_subscription_lifecycle_events.py b/migrations/alembic/versions/0218_podcast_subscription_lifecycle_events.py
--- a/migrations/alembic/versions/0218_podcast_subscription_lifecycle_events.py
+++ b/migrations/alembic/versions/0218_podcast_subscription_lifecycle_events.py
@@ -30,7 +30,7 @@ def upgrade() -> None:
                 WHEN TG_OP = 'DELETE' THEN OLD.id
                 ELSE NEW.id
             END;
-            PERFORM pg_notify('podcast_subscription_events', subscription_identity::text);
+            PERFORM NULL;
             RETURN NULL;
         END;
         $$
