diff --git a/python/nexus/services/tool_authority.py b/python/nexus/services/tool_authority.py
--- a/python/nexus/services/tool_authority.py
+++ b/python/nexus/services/tool_authority.py
@@ -473,7 +473,8 @@
                 return record
 
         async with open_async_session(self.session_factory) as database:
-            return await database.run_sync(prepare)
+            with self.session_factory() as blocking_db:
+                return prepare(blocking_db)
 
     def read_positions(self, db: Session) -> tuple[ToolPositionRecord, ...]:
         rows = db.scalars(
