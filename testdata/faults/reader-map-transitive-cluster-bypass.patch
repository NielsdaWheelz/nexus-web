diff --git a/apps/web/src/components/reader/ReaderDocumentMapOverviewRail.tsx b/apps/web/src/components/reader/ReaderDocumentMapOverviewRail.tsx
--- a/apps/web/src/components/reader/ReaderDocumentMapOverviewRail.tsx
+++ b/apps/web/src/components/reader/ReaderDocumentMapOverviewRail.tsx
@@ -232,7 +232,7 @@
   for (const destination of destinations.sort((left, right) => left.position - right.position)) {
     const cell = Math.floor(destination.position * height / 24);
     const previous = groups.at(-1);
-    if (previous?.cell === cell) previous.members.push(destination);
+    if (previous && destination.position - previous.members.at(-1)!.position < 24 / height) previous.members.push(destination);
     else groups.push({ cell, members: [destination] });
   }
   return groups.map(({ cell, members }) => ({
