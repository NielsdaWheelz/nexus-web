diff --git a/apps/web/src/lib/reader/readerDocumentPosition.ts b/apps/web/src/lib/reader/readerDocumentPosition.ts
--- a/apps/web/src/lib/reader/readerDocumentPosition.ts
+++ b/apps/web/src/lib/reader/readerDocumentPosition.ts
@@ -75,7 +75,7 @@
     ) {
       throw new Error("Text document projection requires ordered unique fragments with canonical lengths.");
     }
-    fragmentOffsets.set(fragment.fragmentId, { start: length, length: fragment.length });
+    fragmentOffsets.set(fragment.fragmentId, { start: 0, length: fragment.length });
     length += fragment.length;
   }
   return { length, fragmentOffsets };
