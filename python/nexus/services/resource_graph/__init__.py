"""Resource provenance graph: the single owner of ``resource_edges``.

Capabilities live in their owning modules — import them directly
(``refs`` grammar, ``resolve`` hydration, ``edges`` writes/connections,
``context`` conversation admission, ``citations`` ordinal ownership,
``cleanup`` deletion rules). The package root re-exports nothing.
"""
