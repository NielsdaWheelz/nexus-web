name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      pull_request_number:
        description: Open same-repository pull request number to recover.
        required: true
        type: string
      expected_head_sha:
        description: Exact 40-character lowercase pull-request head SHA.
        required: true
        type: string
      expected_base_sha:
        description: Exact 40-character lowercase main SHA used as merge base.
        required: true
        type: string

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || inputs.pull_request_number || github.ref }}
  # The persistent runner must let the controller finish its owned process
  # tree. This compatibility workflow predates cancellation-safe evidence
  # staging, so superseded bridge runs queue instead of being killed midway.
  cancel-in-progress: false

permissions: {}

jobs:
  pr:
    name: Deterministic PR proof
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    # GitHub-hosted minutes are unavailable. The dedicated x64 runner executes
    # the same repository-owned gate without changing its proof portfolio.
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 480
    permissions:
      contents: read
      pull-requests: read
    env:
      NEXUS_TEST_BASE_SHA: ${{ github.event_name == 'workflow_dispatch' && inputs.expected_base_sha || (github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before) }}

    steps:
      - name: Validate recovery input syntax
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        env:
          PR_NUMBER: ${{ inputs.pull_request_number }}
          EXPECTED_HEAD_SHA: ${{ inputs.expected_head_sha }}
          EXPECTED_BASE_SHA: ${{ inputs.expected_base_sha }}
        run: |
          set -euo pipefail
          if [[ ! "$PR_NUMBER" =~ ^[1-9][0-9]*$ ]]; then
            echo "pull_request_number must be a canonical positive decimal" >&2
            exit 1
          fi
          if [[ ! "$EXPECTED_HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "expected_head_sha must be a full lowercase Git SHA" >&2
            exit 1
          fi
          if [[ ! "$EXPECTED_BASE_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "expected_base_sha must be a full lowercase Git SHA" >&2
            exit 1
          fi

      # A persistent runner may retain a runtime after a killed job. Recover
      # through the still-checked-out controller's public route so teardown owns
      # the lineage lease before the incoming checkout replaces its schema.
      - name: Retire prior checkout test runtime
        shell: bash
        run: |
          set -euo pipefail
          runtime="$GITHUB_WORKSPACE/.nexus-test/runtime.json"
          if [ ! -e "$runtime" ] && [ ! -L "$runtime" ]; then
            exit 0
          fi
          checkout="$(realpath -e -- "$GITHUB_WORKSPACE")"
          test "$checkout" = "$(realpath -e -- "$(git -C "$checkout" rev-parse --show-toplevel)")"
          test "$(stat -c '%u' -- "$checkout")" = "$(id -u)"
          test ! -L "$checkout/.nexus-test"
          test ! -L "$runtime"
          test "$(stat -c '%u' -- "$runtime")" = "$(id -u)"
          git -C "$checkout" check-ignore --quiet -- .nexus-test/runtime.json
          test -x "$checkout/scripts/test"
          (
            cd "$checkout"
            ./scripts/test clean
          )
          test ! -e "$runtime"
          test ! -L "$runtime"

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('refs/pull/{0}/head', inputs.pull_request_number) || github.sha }}
          fetch-depth: 0
          persist-credentials: false
          # Preserve only ignored controller state on the persistent runner.
          # The proof below clears its own evidence namespace before execution.
          clean: false

      - name: Construct the exact PR merge
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ inputs.pull_request_number }}
          EXPECTED_HEAD_SHA: ${{ inputs.expected_head_sha }}
          EXPECTED_BASE_SHA: ${{ inputs.expected_base_sha }}
        run: |
          set -euo pipefail
          response="$RUNNER_TEMP/nexus-pr-recovery.json"
          gh api \
            --method GET \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER" > "$response"
          jq -e --slurp \
            --argjson number "$PR_NUMBER" \
            --arg repository "$GITHUB_REPOSITORY" \
            --arg base_sha "$EXPECTED_BASE_SHA" \
            --arg head_sha "$EXPECTED_HEAD_SHA" \
            'length == 1
              and (.[0] | type) == "object"
              and .[0].number == $number
              and .[0].state == "open"
              and .[0].base.ref == "main"
              and .[0].base.sha == $base_sha
              and .[0].base.repo.full_name == $repository
              and .[0].head.sha == $head_sha
              and .[0].head.repo.full_name == $repository' \
            "$response" >/dev/null

          checked_out_head="$(git rev-parse HEAD)"
          fetched_main="$(git rev-parse refs/remotes/origin/main)"
          if [ "$checked_out_head" != "$EXPECTED_HEAD_SHA" ]; then
            echo "checked-out PR head does not match expected_head_sha" >&2
            exit 1
          fi
          if [ "$fetched_main" != "$EXPECTED_BASE_SHA" ]; then
            echo "fetched main does not match expected_base_sha" >&2
            exit 1
          fi

          git checkout --detach "$EXPECTED_BASE_SHA"
          git merge --no-ff --no-commit "$EXPECTED_HEAD_SHA"
          merge_timestamp="$(git show --no-patch --format=%cI "$EXPECTED_HEAD_SHA")"
          GIT_AUTHOR_NAME="Nexus CI" \
          GIT_AUTHOR_EMAIL="ci@nexus.local" \
          GIT_AUTHOR_DATE="$merge_timestamp" \
          GIT_COMMITTER_NAME="Nexus CI" \
          GIT_COMMITTER_EMAIL="ci@nexus.local" \
          GIT_COMMITTER_DATE="$merge_timestamp" \
            git commit --no-gpg-sign --message "Synthetic merge for PR #$PR_NUMBER"

          read -r merge_sha first_parent second_parent extra_parent <<EOF
          $(git rev-list --parents -n 1 HEAD)
          EOF
          if [ "$first_parent" != "$EXPECTED_BASE_SHA" ] \
            || [ "$second_parent" != "$EXPECTED_HEAD_SHA" ] \
            || [ -n "${extra_parent:-}" ]; then
            echo "synthetic merge parents do not match expected base then head" >&2
            exit 1
          fi
          if [ "$(git rev-parse HEAD)" != "$merge_sha" ]; then
            echo "synthetic merge identity changed during parent proof" >&2
            exit 1
          fi

      - uses: ./.github/actions/setup-test

      - name: Clear prior runner evidence
        shell: bash
        run: |
          set -euo pipefail
          checkout="$(realpath -e -- "$GITHUB_WORKSPACE")"
          test "$checkout" = "$(realpath -e -- "$(git rev-parse --show-toplevel)")"
          test "$(stat -c '%u' -- "$checkout")" = "$(id -u)"
          git check-ignore --quiet -- test-results/.nexus-ignore-contract
          git clean -qffdx -- test-results/

      - name: Run the deterministic PR gate
        run: ./scripts/test pr

      - name: Upload run evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nexus-test-pr-${{ github.run_id }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 14

      - name: Retire current checkout test runtime
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          runtime="$GITHUB_WORKSPACE/.nexus-test/runtime.json"
          if [ -e "$runtime" ] || [ -L "$runtime" ]; then
            ./scripts/test clean
          fi

      - name: Retire generated web build
        if: always()
        uses: ./.github/actions/clean-generated-web-build

  candidate-full:
    name: Candidate full proof
    if: github.event_name == 'push'
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 480
    permissions:
      contents: read

    steps:
      - name: Retire prior checkout test runtime
        shell: bash
        run: |
          set -euo pipefail
          runtime="$GITHUB_WORKSPACE/.nexus-test/runtime.json"
          if [ ! -e "$runtime" ] && [ ! -L "$runtime" ]; then
            exit 0
          fi
          checkout="$(realpath -e -- "$GITHUB_WORKSPACE")"
          test "$checkout" = "$(realpath -e -- "$(git -C "$checkout" rev-parse --show-toplevel)")"
          test "$(stat -c '%u' -- "$checkout")" = "$(id -u)"
          test ! -L "$checkout/.nexus-test"
          test ! -L "$runtime"
          test "$(stat -c '%u' -- "$runtime")" = "$(id -u)"
          git -C "$checkout" check-ignore --quiet -- .nexus-test/runtime.json
          test -x "$checkout/scripts/test"
          (
            cd "$checkout"
            ./scripts/test clean
          )
          test ! -e "$runtime"
          test ! -L "$runtime"

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
          clean: false

      - uses: ./.github/actions/setup-test
        with:
          android: "true"
          llm-tools: "true"
          provider-runtime: "true"

      - name: Clear prior runner evidence
        shell: bash
        run: |
          set -euo pipefail
          checkout="$(realpath -e -- "$GITHUB_WORKSPACE")"
          test "$checkout" = "$(realpath -e -- "$(git rev-parse --show-toplevel)")"
          test "$(stat -c '%u' -- "$checkout")" = "$(id -u)"
          git check-ignore --quiet -- test-results/.nexus-ignore-contract
          git clean -qffdx -- test-results/

      - name: Run the candidate full gate
        run: ./scripts/test full

      - name: Upload run evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nexus-test-full-${{ github.run_id }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 14

      - name: Retire current checkout test runtime
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          runtime="$GITHUB_WORKSPACE/.nexus-test/runtime.json"
          if [ -e "$runtime" ] || [ -L "$runtime" ]; then
            ./scripts/test clean
          fi

      - name: Retire generated web build
        if: always()
        uses: ./.github/actions/clean-generated-web-build
