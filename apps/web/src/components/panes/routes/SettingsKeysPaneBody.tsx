"use client";

import AppSettingsKeysPaneBody from "@/app/(authenticated)/settings/keys/SettingsKeysPaneBody";

export default function SettingsKeysPaneBody() {
  return <AppSettingsKeysPaneBody />;
}
