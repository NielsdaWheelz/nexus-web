// Shared bound for automatic role-slice replacement (see
// python/nexus/services/contributor_taxonomy.py MAX_CREDITS_PER_MANAGED_ROLE — the two
// literals are pinned equal by a static gate in test_cutover_negative_gates.py).
export const MAX_CREDITS_PER_MANAGED_ROLE = 20;
