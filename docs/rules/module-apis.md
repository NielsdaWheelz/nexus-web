# Module APIs

## Scope

This document covers how modules should expose capabilities.

## Rules

- Expose each capability in one primary form.
- Do not expose interchangeable duplicate APIs for the same capability.
- If a capability already exists in a module, prefer using it over introducing a near-duplicate.
