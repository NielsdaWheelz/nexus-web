status: open
origin: 2026-09-14 bounded workspace sparse table activation
area: native schema1 conversion

the original converter emitted no sparse table context or metadata. actual
source geometry/header staging now passes `d206e6f46f159866` at `299360435f`.
`OfflineReadingLegacyTablePublication.kt` drafts unit excerpts, exact source
ranges, later caption finalization and sparse metadata; actual conversion/preparation passed `b36bfbe2311d361d` at `91069774aa`,
including fitting whole-table sensitivity, large spans, a split cell, empty cell
and later caption. opening-only continuation and original-spool suffix projection now pass
`3e0a6b9f03eed27e`, `0870450a5ac7cc35` and `e952b5bace006ca8` after the
actual trailing-separator and pending-space/combining regressions. final
interrupted actual-table activation is canonical green `e46caec8eead21cc`:
original source/row/pending progress survive the injected precommit failure,
offline retry publishes an exact private-index digest and reopens the source.
the independently expected full header corpus through actual legacy source,
maximum table cost and device qualification remain open. hosted producer
fixtures cannot replace that native source owner.

the incoming/retained adapter and index lifecycle are implemented. complete
the actual staged legacy html/source.sqlite header-corpus replay and qualify
source limits, preserving source dom order, zero-text unit identities, caption
ranges and explicit headers. keep preparation within existing conversion
staging/activation and original-source recovery. this remains required by the
active specification, not a post-release enhancement.

acceptance: real installed schema1 tables convert through the actual local source
owner, produce the same independently expected header corpus, reopen with an exact
revision-bound index and preserve pending progress through interruption/retry.
qualify supported source limits, derived disk and actual device cost.

actual native source geometry/header associations now pass the independent
normalized corpus: canonical `da35b9ad3bb79af8` at `905f5b7f52`, after the
scope-loss fault reaches its named oracle. this closes the source/header corpus
slice; aggregate metadata and selected-query maximum/device gates remain open.

2026-09-14 scope correction: the user waived physical-handset verification on
this vps. native compilation and host qualification remain required; no device
behavior or capacity claim is made.
