# Slice 6 PR-04 Decisions

## Decision Ledger

| id | question | decision | rationale | fallback/default | owner | due | status |
|---|---|---|---|---|---|---|---|
| S6-PR04-D01 | Where should `pr-04` place PDF geometry canonicalization/fingerprinting logic and how should pure canonicalization be separated from DB write orchestration? | Dedicated pure geometry module (`python/nexus/services/pdf_highlight_geometry.py`) for canonicalization/fingerprinting/sort-key derivation, with transactional DB validation/persistence/duplicate/coherence enforcement in highlight service orchestration (`python/nexus/services/pdf_highlights.py` under `S6-PR04-D04`, reached via the generic `highlights.py` layer). | Produces one authoritative deterministic geometry implementation that is easy to unit test, prevents create/update drift, and keeps transactional correctness centralized. Best production-ready MVP split without over-abstracting. | If implementation friction appears, keep the pure-geometry vs transactional-service split and extract helpers within those modules; do not move canonicalization into routes or duplicate it across codepaths. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D02 | What exact race-safe duplicate enforcement strategy should `pr-04` use for PDF highlight writes (transactional enforcement only vs schema/index refinement now) while honoring the `pr-01` deferred-hardening posture? | Service-level transactional duplicate enforcement using canonical geometry identity with a transaction-scoped Postgres advisory lock + duplicate recheck, with no schema/index refinement in `pr-04` unless implementation proves it necessary. | Delivers exact duplicate race safety without schema churn, preserves the deferred-hardening posture from `pr-01`, and uses a precise lock scope keyed to canonical duplicate identity rather than coarse media/page locking. Clean production-ready MVP with strong concurrency semantics and clear tests. | If implementation friction appears, preserve transactional duplicate race safety with duplicate recheck semantics (coarser transactional locking is acceptable as a fallback); do not ship a naive check-then-insert race window or force schema refinement into `pr-04` without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D03 | How should `pr-04` evolve the public highlight response schema(s) so generic routes can return typed PDF highlights while preserving fragment-route compatibility for existing clients/tests? | Introduce an explicit anchor-discriminated generic highlight response schema for generic highlight routes and PDF highlight routes, while preserving fragment-route response compatibility (including legacy fragment fields where required by fragment-route contracts/tests). | Satisfies typed-highlight requirements for `pr-04` without breaking established fragment-route clients/tests. Creates a clean migration path: generic routes become typed/future-proof while fragment routes remain stable during the PDF rollout. | If implementation friction appears, preserve the split between typed generic responses and fragment-route compatibility; do not leave generic routes fragment-only for PDF highlights or force a full fragment-route response migration in `pr-04` without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D04 | How should `pr-04` split PDF highlight write/read logic between `python/nexus/services/highlights.py` and any new PDF-specific service helper/module while preserving the `D01` pure-geometry split and `pr-02` kernel reuse? | Introduce a dedicated `python/nexus/services/pdf_highlights.py` module for PDF highlight create/list/update transactional orchestration, while keeping `python/nexus/services/highlights.py` as the generic/fragment orchestration layer and route-facing compatibility surface. | Keeps `pr-04` reviewable and maintainable without a broad service refactor, concentrates PDF-specific complexity in one module, and preserves established generic-route/`highlight_kernel` seams. Best production-ready MVP split for this PR. | If implementation friction appears, preserve the `pdf_highlights.py` vs `highlights.py` split; do not collapse PDF orchestration back into `highlights.py` or undertake a full highlight-service re-architecture in `pr-04` without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D05 | How should `pr-04` evolve the generic `PATCH /highlights/{highlight_id}` request schema/validation to support both fragment offset updates and PDF bounds replacement while preserving fragment-client compatibility and preventing ambiguous mixed payloads? | Use a backward-compatible unified PATCH schema that preserves existing fragment fields (`start_offset`, `end_offset`, `color`) and adds a nested `pdf_bounds` payload for PDF bounds replacement, with strict mutual exclusivity between fragment offsets and `pdf_bounds`, plus service-level anchor-kind validation/dispatch. | Preserves fragment PATCH compatibility, prevents ambiguous mixed payloads, and makes PDF bounds replacement explicit and testable. Keeps transport validation clear while allowing typed service dispatch for fragment vs PDF highlights. | If implementation friction appears, preserve the unified backward-compatible PATCH contract and mutual-exclusivity guarantees; do not require a new discriminated PATCH request contract in `pr-04` or allow ambiguous flat mixed payloads without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D06 | What exact `pr-04` write-time behavior should PDF highlight create/bounds-update use for `plain_text_match_*` metadata and `prefix/suffix` fields (attempt deterministic match enrichment now vs initialize/reset pending and defer enrichment to `pr-05`)? | `pr-04` implements S6 write-time PDF match metadata computation on create/bounds-update when PDF quote-text infrastructure is ready, and persists/resets `pending` + empty `prefix/suffix` only when quote-text infrastructure is not ready. `pr-05` owns quote rendering usage and follow-on enrichment paths, not the core write-time storage contract. | Aligns `pr-04` with the normative S6 write-path contract and keeps highlight write persistence semantically correct as soon as quote text is ready. Prevents pushing core write-path match semantics into the quote-rendering PR while preserving `pr-05` ownership of quote usage/enrichment behavior. | If implementation friction appears, preserve synchronous write-time match metadata/prefix-suffix computation semantics for ready PDFs and `pending` fallback for not-ready PDFs; do not defer all ready-PDF writes to `pending` without an explicit L2/L3 change. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D07 | Where should the deterministic PDF quote-match algorithm/prefix-suffix derivation helper live for `pr-04` writes and later `pr-05` enrichment/quote paths (reuse boundary to avoid duplication)? | Shared pure helper module `python/nexus/services/pdf_quote_match.py`, reused by `pdf_highlights.py` in `pr-04` and quote/enrichment paths in `pr-05`. | Prevents algorithm drift across write-time persistence and quote/enrichment paths, keeps the matcher pure and unit-testable, and establishes one authoritative implementation of S6 `plain_text_match_version=1` semantics. Best production-ready MVP reuse boundary. | If implementation friction appears, preserve one shared pure helper module for deterministic PDF matching and `prefix/suffix` derivation; do not duplicate the algorithm across `pdf_highlights.py` and quote services or push DB/logging concerns into the matcher without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D08 | What exact deterministic hashing/serialization contract should `pr-04` use for persisted `geometry_fingerprint` and advisory-lock key derivation so duplicate identity is stable and concurrency behavior is testable? | Canonical geometry identity bytes in `pdf_highlight_geometry.py`; persisted `geometry_fingerprint` is SHA-256 lowercase hex over canonical identity bytes; advisory-lock key is a stable namespaced deterministic `int64` derived from the full duplicate identity, with duplicate recheck preserved for correctness. | Prevents unstable process-dependent hashing, makes fingerprint and lock-key derivation deterministic/testable across environments, and preserves `D02` correctness even if advisory-lock key collisions occur (duplicate recheck remains authoritative). Best production-ready MVP hash/lock contract without schema churn. | If implementation friction appears, preserve deterministic canonical byte serialization + non-process-dependent hashing semantics and keep duplicate recheck authoritative; do not use Python `hash()` or implicit object/float string hashing for persisted fingerprints or lock keys. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D09 | What transaction sequencing and advisory-lock scope should `pr-04` use for PDF create/bounds-update so `D02` duplicate race safety, `D06` write-time match persistence, and `D07` shared matcher reuse remain atomic while lock contention stays bounded? | Canonicalize geometry and compute deterministic match results before advisory-lock acquisition; acquire a media-scoped coordination lock first, then the duplicate-identity advisory lock; perform duplicate recheck + atomic persistence under locks; keep duplicate-lock hold time bounded and duplicate recheck authoritative. | Balances correctness and throughput: exact duplicate race safety remains intact, write-time match persistence is atomic with persisted highlight rows, and lock contention stays bounded by keeping pure canonicalization/matching work outside the duplicate lock. The explicit lock-ordering contract also supports safe interop with `pr-03` text rebuild/invalidation paths. | If implementation friction appears, preserve the lock-ordering contract (media coordination lock -> duplicate lock) and duplicate recheck authority, and keep duplicate-lock hold time minimized; do not hold duplicate locks across expensive pure matching/canonicalization work without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D10 | Where should the shared media-scoped coordination lock helper live so `pr-04` PDF highlight writes and `pr-03` internal PDF text-rebuild/invalidation paths reuse the same lock namespace/ordering contract without duplication or drift? | A small shared helper module `python/nexus/services/pdf_locking.py` owns media-scoped coordination lock key derivation and lock-order helper(s), and is reused by `pdf_highlights.py` in `pr-04` plus narrow `pr-03` interop patches. | Prevents lock namespace/order drift across `pr-03`/`pr-04`, avoids cross-domain coupling between highlight and lifecycle/ingest services, and keeps concurrency correctness in one low-level reusable seam. Best production-ready MVP placement for the shared coordination lock contract. | If implementation friction appears, preserve one shared low-level PDF locking helper module for coordination-lock key derivation/order helpers; do not duplicate lock-key/order logic across `pdf_highlights.py` and `pdf_lifecycle`/`pdf_ingest` without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D11 | How should `pr-04` split duplicate-identity advisory-lock key ownership between `pdf_highlight_geometry.py` (`D08`) and `pdf_locking.py` (`D10`) so lock helpers are reusable without redefining canonical geometry identity? | `pdf_highlight_geometry.py` owns canonical geometry identity + duplicate-identity advisory-lock key primitives; `pdf_locking.py` owns media coordination lock keys and generic ordered lock acquisition helpers, and may wrap duplicate-lock acquisition only by calling geometry-owned key derivation. | Preserves `D08` canonical-identity ownership, keeps `pdf_locking.py` low-coupling and reusable, prevents duplicate-lock key drift, and cleanly separates pure identity semantics from lock mechanics. Best production-ready MVP boundary between geometry and locking helpers. | If implementation friction appears, preserve geometry-owned duplicate-key derivation and keep `pdf_locking.py` limited to coordination-lock keys + lock mechanics; do not duplicate canonical duplicate-identity logic across modules without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D12 | What error policy should `pr-04` use when write-time PDF quote-match computation (`D06`/`D07`) encounters unexpected anomalies on quote-ready media (matcher exception or inconsistent text artifacts) during create/bounds-update? | Classified hybrid policy: deterministic matcher outcomes persist normally; recoverable/classified matcher anomalies on quote-ready media degrade to `plain_text_match_status='pending'` with cleared offsets/version and empty `prefix/suffix` plus structured anomaly logging; unexpected/unclassified exceptions fail the mutation (`500`) with no partial write. | Preserves highlighting UX for recoverable artifact anomalies while maintaining strong engineering integrity for real bugs. Aligns with S6 degrade-safe quote semantics and fits `D09` atomic write guarantees (no partial highlight writes on unexpected matcher failures). | If implementation friction appears, preserve the approved fail-open/fail-closed split (recoverable anomaly => `pending`, unclassified exception => fail closed) and no-partial-write guarantee; do not switch to blanket fail-open or blanket fail-closed without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D13 | Where should write-time PDF matcher anomaly classification types and structured anomaly logging boundaries live so `D12` is implemented consistently across `pr-04` writes and later `pr-05` enrichment without violating `D07` (pure matcher, no logging)? | Two-layer split: `pdf_quote_match.py` remains pure/log-free and exposes typed recoverable anomaly classifications; shared `python/nexus/services/pdf_quote_match_policy.py` owns structured anomaly logging and path-specific mapping helpers reused by `pr-04` write paths and `pr-05` quote/enrichment paths. | Preserves `D07` matcher purity while preventing `pr-04`/`pr-05` drift on anomaly classification/logging policy. Keeps pure classification logic unit-testable and service-policy observability/mapping testable without contaminating the matcher with logging or transport concerns. | If implementation friction appears, preserve the two-layer split (pure matcher classification + shared policy logging/mapping). Do not move logging into `pdf_quote_match.py` or duplicate anomaly taxonomies/log schemas in `pdf_highlights.py`/`pr-05` services without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D14 | What exact structured anomaly log event schema and helper API contract should `pdf_quote_match_policy.py` expose so `pr-04` and `pr-05` reuse one observability surface for matcher anomalies without log drift or double-logging? | `pdf_quote_match_policy.py` owns the canonical `pdf_quote_match_anomaly` structured event schema (required anomaly classification + target context fields, optional non-sensitive diagnostics), centralized logging/mapping helper APIs, and an explicit no-double-logging rule for callers; helper APIs return typed recoverable outcomes and raise typed internal failures for unclassified cases. | Establishes one stable observability surface for matcher anomalies across write-time and quote/enrichment flows, prevents duplicate anomaly event noise, and enforces clear logging ownership in the shared policy layer introduced by `D13`. Strong production-ready MVP observability contract without introducing a generic logging framework. | If implementation friction appears, preserve the canonical event name/schema and centralized helper logging ownership in `pdf_quote_match_policy.py`; do not let callers define local anomaly event names/fields or re-log the same anomaly decision without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D15 | What exact redaction/privacy contract should `pdf_quote_match_policy.py` anomaly logging follow for document-derived text inputs (`exact`, `prefix/suffix`, `media.plain_text`) so observability remains useful without leaking user content? | Strict no-content anomaly logging in MVP: no raw document-derived text (`exact`, `prefix`, `suffix`, `media.plain_text` excerpts) and no unsalted text hashes; `pdf_quote_match_policy.py` sanitizes exception details/messages and emits only approved non-content diagnostics (codes, ids, booleans, presence flags, lengths/counts, and other non-sensitive fields). | Strong production-safe privacy posture with minimal complexity, while retaining useful structured diagnostics for debugging. Prevents accidental user-content leakage in telemetry and keeps the policy consistent across `pr-04` and `pr-05` reuse. | If implementation friction appears, preserve the no-raw-text/no-unsalted-hash contract and centralized sanitization in `pdf_quote_match_policy.py`; do not add raw excerpts or default text hashes without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D16 | What exact error-code/status mapping should generic `PATCH /highlights/{id}` use when a syntactically valid `pdf_bounds` payload targets a non-PDF highlight (or fragment offsets target a PDF highlight) under the unified backward-compatible PATCH schema? | Service-level anchor-kind dispatch rejects cross-kind but syntactically valid unified PATCH payloads with `400 E_INVALID_REQUEST` (after visibility/resource resolution), returns deterministic machine-readable anchor-kind mismatch details, and applies no mutation. | Keeps `D05` transport validation focused on shape while classifying cross-kind payloads correctly as client semantic errors. Produces deterministic generic PATCH behavior across fragment/PDF highlights without introducing a narrow new error code. | If implementation friction appears, preserve service-level semantic rejection with `400 E_INVALID_REQUEST`, no mutation, and deterministic anchor-kind mismatch details; do not remap these cases to `409` or `500` without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D17 | What exact idempotency/no-op contract should generic `PATCH /highlights/{id}` use for PDF bounds replacements that canonicalize to the target highlight’s existing geometry identity? | Self-same canonical geometry is not a duplicate conflict (duplicate checks exclude target highlight id); generic PATCH succeeds and remains a normal success path, with write-time derived fields recomputed/persisted when mutable inputs change and idempotent no-op success allowed when effective state is unchanged. | Preserves idempotent retries, avoids false duplicate conflicts on semantically identical geometry, and keeps `D06`/`D12` write-time derived field correctness intact. Allows implementations to optimize away unnecessary quad churn without changing external behavior. | If implementation friction appears, preserve the no-conflict self-same-geometry rule and success semantics; do not treat self-same canonical geometry as `E_HIGHLIGHT_CONFLICT` or introduce a failure path without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D18 | What exact timestamp/persistence semantics should generic `PATCH /highlights/{id}` use for fully identical effective updates (self-same canonical geometry and no effective field changes) on PDF highlights? | Fully identical effective PATCH is a true no-op success (`200`) with no persisted-state mutation, no `updated_at` bump, and no quad/subtype row rewrites; if any effective mutable value differs, normal update semantics apply (including `updated_at` changes). | Strong idempotency semantics with clean audit/cache behavior and no unnecessary DB churn, while preserving normal update behavior when anything actually changes. Keeps PATCH behavior deterministic for retries and tests. | If implementation friction appears, preserve the observable no-op contract (success with unchanged persisted state/timestamps); do not rewrite/bump `updated_at` for fully identical effective updates without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D19 | May `pr-04` short-circuit fully identical effective PDF PATCH requests before acquiring the `D09` media/duplicate advisory locks (to reduce lock contention), and if so under what safety conditions? | Guarded hybrid policy: optional pre-`D09` no-op short-circuit is allowed only after acquiring a transaction-scoped target-row lock and safely proving fully identical effective state (including no derived-field recomputation need); otherwise the normal `D09` lock sequence is used, while `D18` no-op semantics remain observable either way. | Preserves `D18` no-op correctness while avoiding stale-snapshot short-circuits under concurrency. Reduces lock contention for true no-ops without requiring the optimization and keeps `D09` as the safe fallback path. | If implementation friction appears, skip the optimization and always use `D09`; preserve `D18` observable no-op semantics and do not introduce unsafe pre-lock short-circuits without target-row-lock + safe-equality-proof conditions. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR04-D20 | Should `pr-04` require a single canonical service helper/path for PDF PATCH effective-state comparison (used by both pre-`D09` short-circuit checks and post-lock no-op detection) to avoid drift in “effective equality” logic? | Require one canonical deterministic side-effect-free effective-state comparison helper/path in `pdf_highlights.py` (or an equivalent narrowly scoped helper in the same service boundary), reused by guarded pre-lock short-circuit gating and post-lock no-op detection; helper returns structured comparison results including an explicit `requires_full_path`/safe-fallback outcome when equality cannot be proven. | Prevents equality/no-op drift across optimized vs fallback PATCH branches, keeps `D17`/`D18`/`D19` semantics consistent (including no-op detection and timestamp behavior), and provides a single testable correctness seam. Strong production-ready MVP discipline for a subtle idempotency path without forcing a larger refactor. | If implementation friction appears, preserve one canonical comparison logic path and the explicit fallback-to-`D09` outcome; do not duplicate ad hoc effective-equality checks across branches or replace the structured outcome with ambiguous booleans without a new decision. | Spec owner + platform | pr-04 drafting | Accepted (user-approved 2026-02-25) |

## Notes

- `pr-04` depends on `pr-02` (typed-highlight kernel compatibility) and `pr-03` (PDF lifecycle/readiness semantics).
- Accepted `pr-04` decisions that affect later PRs must be propagated to `docs/v1/s6/s6_pr_roadmap.md` carry-forward notes.
- `pr-04` must preserve the `pr-02` `highlight_kernel` side-effect-free resolver posture and centralized mismatch logging/mapping contract on generic routes.
- `S6-PR04-D01` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses a dedicated pure `pdf_highlight_geometry.py` module for canonicalization/fingerprinting/sort-key derivation, while highlight service orchestration (`pdf_highlights.py` under `S6-PR04-D04`, via `highlights.py`) owns transactional persistence/coherence/duplicate enforcement.
- `S6-PR04-D02` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses service-level transactional duplicate race safety with a transaction-scoped advisory lock + duplicate recheck, without schema/index refinement unless implementation proves it necessary.
- `S6-PR04-D03` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` introduces anchor-discriminated typed responses for generic/PDF highlight routes while preserving fragment-route response compatibility.
- `S6-PR04-D04` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses a dedicated `pdf_highlights.py` orchestration module for PDF highlight create/list/update logic while `highlights.py` remains the generic/fragment compatibility layer.
- `S6-PR04-D05` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses a backward-compatible unified generic PATCH schema with nested `pdf_bounds` and strict mutual exclusivity against fragment offset updates.
- `S6-PR04-D06` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` computes deterministic PDF match metadata + `prefix/suffix` on create/bounds-update when quote-text infrastructure is ready, and uses `pending` + empty `prefix/suffix` fallback only when not ready.
- `S6-PR04-D07` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` and `pr-05` share a pure `pdf_quote_match.py` helper for deterministic PDF match-status/offset and `prefix/suffix` derivation, preventing algorithm drift across write-time persistence and quote/enrichment paths.
- `S6-PR04-D08` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses canonical geometry identity byte serialization + SHA-256 lowercase hex `geometry_fingerprint` and a stable namespaced deterministic `int64` advisory-lock key (with duplicate recheck authoritative) for race-safe duplicate enforcement.
- `S6-PR04-D09` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses a two-lock sequencing contract (media coordination lock -> duplicate lock), computes geometry/match results before duplicate-lock acquisition, and keeps duplicate-lock hold time bounded to duplicate recheck + atomic persistence.
- `S6-PR04-D10` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` and the narrow `pr-03` interop patch share a low-level `pdf_locking.py` module for media coordination lock key derivation and lock-order helpers to avoid namespace/order drift.
- `S6-PR04-D11` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pdf_highlight_geometry.py` remains the authority for canonical duplicate-identity/duplicate-lock key derivation, while `pdf_locking.py` owns coordination-lock keys and lock mechanics.
- `S6-PR04-D12` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` uses a classified hybrid write-time matcher anomaly policy on quote-ready media: recoverable/classified anomalies degrade to `pending` + empty `prefix/suffix` with structured logging, while unexpected/unclassified exceptions fail the mutation (`500`) with no partial write.
- `S6-PR04-D13` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pdf_quote_match.py` remains pure/log-free and exposes typed recoverable anomaly classifications, while shared `pdf_quote_match_policy.py` owns structured anomaly logging and path-specific mapping helpers reused by `pr-04` and `pr-05`.
- `S6-PR04-D14` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pdf_quote_match_policy.py` owns the canonical `pdf_quote_match_anomaly` event schema, centralized logging/mapping helper APIs, and the no-double-logging caller contract for matcher anomalies.
- `S6-PR04-D15` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pdf_quote_match_policy.py` enforces a strict no-content anomaly logging contract in MVP (no raw text or unsalted text hashes, sanitized exception diagnostics, approved non-content fields only).
- `S6-PR04-D16` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; generic unified PATCH cross-kind but syntactically valid payloads are rejected by service-level anchor-kind dispatch with `400 E_INVALID_REQUEST`, deterministic anchor-kind mismatch details, and no mutation after visibility/resource resolution.
- `S6-PR04-D17` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; self-same canonical PDF bounds-updates are not duplicate conflicts and PATCH succeeds, with recomputation of write-time derived fields when mutable inputs change and idempotent no-op success allowed when effective state is unchanged.
- `S6-PR04-D18` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; fully identical effective PDF PATCH updates are true no-op successes with no persisted-state mutation, no `updated_at` bump, and no quad/subtype row rewrites.
- `S6-PR04-D19` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; optional pre-`D09` no-op short-circuiting is allowed only under guarded conditions (transaction-scoped target-row lock + safe equality proof), otherwise the normal `D09` path is used and `D18` no-op semantics remain observable.
- `S6-PR04-D20` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr04_spec.md`; `pr-04` requires one canonical deterministic side-effect-free effective-state comparison helper/path reused by guarded pre-lock short-circuit gating and post-lock no-op detection, with an explicit structured fallback-to-`D09` outcome when equality cannot be safely proven.
