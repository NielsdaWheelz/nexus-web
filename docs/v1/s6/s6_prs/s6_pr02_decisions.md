# Slice 6 PR-02 Decisions

## Decision Ledger

| id | question | decision | rationale | fallback/default | owner | due | status |
|---|---|---|---|---|---|---|---|
| S6-PR02-D01 | What is the canonical fragment-anchor source-of-truth and read/write strategy in `pr-02` under the `pr-01` transitional bridge (`highlights.fragment_*` + `highlight_fragment_anchors`) while preserving fragment-route product semantics? | Typed logical fields + `highlight_fragment_anchors` are canonical for fragment anchor semantics in `pr-02`; legacy `highlights.fragment_*` remains a compatibility mirror. Fragment create/update paths perform transactional service-level dual-write (no triggers). Read paths prefer typed data, tolerate `dormant_repairable` rows in read-only flows (per `D06`), and fail safe on irreconcilable bridge-vs-subtype mismatches (no silent precedence choice). Dormant-window repair/normalization occurs in explicit write-capable service paths / repair helpers (per `D02`). | Delivers a real typed-kernel migration in `pr-02` while preserving fragment-route behavior and duplicate semantics. Greenfield production removes backfill needs but not rollout-window compatibility risk. Fail-safe conflict handling protects against silent data drift. | If implementation friction appears, keep typed source-of-truth + dual-write policy and narrow explicit repair call sites rather than reverting to legacy-canonical reads or adding hidden read-path repairs. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D02 | Where is `pr-02` allowed to perform dormant-window fragment repair/normalization (including whether read-only helpers may write), and what is the side-effect policy for shared kernel resolvers? | Shared logical highlight resolver/media-resolution helpers are side-effect free (no writes/commits/implicit repair). Dormant-window repair occurs only via explicit transactional `pr-02` repair helper(s) called from approved write-capable service paths (fragment create/update and other explicitly selected mutation paths) and optional explicit repair utilities used by tests/admin tooling. | Preserves clean read-vs-write boundaries, predictable transactions, and stable tests while still satisfying `pr-02`'s tolerate+repair ownership. Prevents hidden writes in auth/context/send-message read helpers. | If implementation friction appears, broaden explicit repair call sites while keeping shared resolvers side-effect free; do not add writes to auth/context read helpers. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D03 | What exact fail-safe error behavior should `pr-02` use for irreconcilable fragment bridge-vs-subtype mismatches across bool helpers, read routes, and owner mutation paths? | Use path-specific fail-safe mapping: shared bool visibility helpers fail closed (`False`); no-existence-leak user-facing visibility/read gating paths map mismatch to masked not-found behavior; owner-authorized mutation paths and trusted internal write/repair services raise explicit internal integrity failure. Fragment-scoped read/list paths do not silently skip or auto-heal mismatches. | Preserves no-existence-leak guarantees and existing bool-helper contracts while surfacing real corruption loudly on trusted paths. Provides deterministic behavior for tests and avoids silent drift. | If implementation friction appears, centralize mapping logic in a shared helper while preserving the same path-specific mapping classes. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D04 | What module/API shape should `pr-02` use for the shared logical highlight resolver + typed serializer seams to avoid duplication and circular imports across `highlights`, `permissions`, `contexts`, and `send_message`? | Introduce a dedicated internal kernel module (`python/nexus/services/highlight_kernel.py`) that owns structured side-effect-free logical highlight resolution, mismatch classification, explicit transactional fragment repair helper(s), and internal typed highlight view/serializer seams. The module imports only models/errors/SQLAlchemy/stdlib and is reused by `highlights`, `permissions`, `contexts`, `send_message`, and later `pr-04`/`pr-05`. | Prevents circular imports, centralizes `D01/D02/D03` semantics, and provides a stable reusable seam for later PRs without coupling kernel logic to public route schemas. | If implementation friction appears, keep the dedicated module and phase in resolver+mismatch typing first, then repair/typed-view helpers; do not scatter logic back into consumer modules. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D05 | What implementation strategy should `pr-02` use for `contexts.recompute_conversation_media` after removing fragment-only joins: SQL-first set-based query rewrite, shared-kernel resolver iteration, or a hybrid batch strategy? | Use a hybrid batch strategy: bulk-load message contexts and referenced highlights/annotations, resolve media via side-effect-free `highlight_kernel` semantics in Python, tolerate `dormant_repairable` rows without hidden repair writes, raise explicit internal integrity failure on mismatches, then diff/apply `conversation_media` updates. | Reuses canonical kernel semantics (no drift), preserves `D02` side-effect boundaries and `D03` mismatch behavior, and avoids brittle raw-SQL duplication or naive N+1 iteration. | If performance pressure appears, optimize batch loading/query shape while preserving kernel-semantic reuse and side-effect-free resolver behavior; do not revert to duplicated fragment-only raw SQL anchor logic. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D06 | What is the exact shared-resolver contract for `dormant_repairable` fragment rows in read-only consumers: may they proceed using legacy-bridge-derived media/fragment anchor data, or must they fail closed until explicit repair? | `highlight_kernel` may return resolved media + fragment anchor data for fragment-backed rows in `state='dormant_repairable'` when legacy bridge fields are self-consistent and sufficient. Read-only consumers (auth/context resolution/send-message visibility/read routes) may proceed using that resolved data without hidden repair writes, while emitting observability for deferred repair. Write-capable fragment service paths explicitly repair before continuing mutation logic where `pr-02` policy requires canonical synchronization. | Preserves pre-S6 fragment behavior across rollout windows without hidden writes while keeping `dormant_repairable` distinct from true mismatches. Aligns with greenfield production and existing code/test compatibility needs. | If implementation friction appears, keep resolver semantics and narrow which read-only consumers proceed; do not collapse `dormant_repairable` into `mismatch`. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR02-D07 | What exact internal integrity exception / logging contract should `pr-02` use for `highlight_kernel` mismatch outcomes so `D03` path-specific mapping is consistent across services? | Keep the resolver pure/log-free and centralize mismatch handling in `highlight_kernel` mapping helper(s). Define a dedicated internal kernel integrity exception (recommended `HighlightKernelIntegrityError`, `E_INTERNAL` semantics) carrying structured diagnostics (`mismatch_code`, target ids, `consumer_operation`, `mapping_class`). Centralized mapping helper(s) implement `D03` path-specific mapping (bool fail-closed / masked not-found / internal error) and emit one canonical structured mismatch log event (`highlight_kernel_mismatch`) per mapping decision; consumer modules reuse the helper and do not duplicate mismatch logs. | Preserves `D02` resolver purity and enforces `D03` consistently across services while preventing logging/mapping drift. Provides a stable, reusable mismatch observability contract for later `pr-04`/`pr-05` reuse. | If implementation friction appears, keep centralized kernel mapping/logging helpers + typed internal exception and phase optional diagnostic fields later; do not move mismatch logging/mapping back into consumer modules. | Spec owner + platform | pr-02 drafting | Accepted (user-approved 2026-02-24) |

## Notes

- Greenfield production assumption means no production data backfill is required, but `pr-02` must still tolerate rows created during a `pr-01` -> `pr-02` rollout window.
- `pr-01` established the schema and dormant-field bridge; `pr-02` is the first PR that adopts typed-highlight kernel behavior and therefore owns compatibility repair semantics for fragment highlights.
- `S6-PR02-D01` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; remaining `pr-02` drafting should not re-open typed-canonical fragment anchors + service-level dual-write without explicit review.
- `S6-PR02-D02` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; shared logical highlight resolvers remain side-effect free and dormant-window repair stays in explicit write-capable service paths.
- `S6-PR02-D03` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; path-specific fail-safe mismatch mapping now constrains bool helpers, masked user-facing visibility surfaces, and owner/internal write paths.
- `S6-PR02-D04` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; `python/nexus/services/highlight_kernel.py` is the dedicated shared resolver/typing/repair seam for `pr-02` and later PR reuse.
- `S6-PR02-D05` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; `recompute_conversation_media` now has a hybrid batch strategy that reuses `highlight_kernel` semantics and preserves side-effect-free resolver behavior.
- `S6-PR02-D06` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; read-only consumers may proceed on `dormant_repairable` rows using resolver-returned legacy-derived data without hidden repair writes, with observability for deferred repair.
- `S6-PR02-D07` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr02_spec.md`; `highlight_kernel` now owns the centralized mismatch logging/mapping helper contract and typed internal integrity exception diagnostics used to enforce `D03` consistently.
- Future `pr-02` decisions must also be reflected in `docs/v1/s6/s6_pr_roadmap.md` carry-forward notes when they affect later PR ownership or constraints.
