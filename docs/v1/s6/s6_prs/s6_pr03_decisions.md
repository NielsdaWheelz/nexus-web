# Slice 6 PR-03 Decisions

## Decision Ledger

| id | question | decision | rationale | fallback/default | owner | due | status |
|---|---|---|---|---|---|---|---|
| S6-PR03-D01 | What lifecycle orchestration/module shape should `pr-03` use for PDF ingest confirm/retry and async extraction completion: extend `epub_lifecycle`, add a dedicated `pdf_lifecycle` + `ingest_pdf`, or introduce a generic cross-kind file-lifecycle framework now? | Dedicated PDF lifecycle split: `python/nexus/services/pdf_lifecycle.py` + `python/nexus/services/pdf_ingest.py` + `python/nexus/tasks/ingest_pdf.py`, with thin route-level branching in `python/nexus/api/routes/media.py`. | Matches the proven EPUB split, preserves policy isolation between EPUB and PDF lifecycle semantics, and avoids premature generic lifecycle framework work in a behavior-heavy PR. Minimizes regression risk by keeping EPUB lifecycle code stable. | If implementation friction appears, extract small shared helpers after duplication is concrete, but keep distinct PDF lifecycle/task modules in `pr-03` (do not merge PDF policy into `epub_lifecycle`). | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D02 | Which PDF extraction engine and deterministic error-classification contract should `pr-03` standardize for S6 (including password-protected vs renderable-no-text outcomes), and where should parser-specific behavior be isolated? | Use **PyMuPDF** in `pr-03`, with all parser-specific behavior and exception mapping isolated in `python/nexus/services/pdf_ingest.py`. Lifecycle/task layers consume only parser-agnostic typed outcomes (`success_with_text`, `success_no_text`, `protected_failure`, `extract_failure`). | Aligns with S6’s expected implementation while preserving parser-agnostic lifecycle/task/API behavior. Improves testability and prevents parser exception semantics from leaking into retry/lifecycle logic. | If implementation friction appears, keep PyMuPDF and parser isolation and simplify internal outcome modeling, but preserve parser-agnostic behavior at lifecycle/task boundaries. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D03 | What retry/rebuild mode contract should `pr-03` implement for PDF (`POST /media/{id}/retry` vs internal rebuild paths) so S6 invalidation rules and “no text rewrite on embedding/search retry” are both enforceable without breaking existing route compatibility? | Keep public `POST /media/{id}/retry` request/response compatibility unchanged and infer user-facing retry behavior from lifecycle/failure context; implement explicit internal `pdf_lifecycle` rebuild/repair helper(s) for text-artifact rewrites/operator rebuild paths. Public retry paths enforce S6 mode-specific behavior without a new API parameter. | Preserves route compatibility and client simplicity while enforcing the S6 split between embedding/search no-rewrite retries and text-rebuild invalidating retries. Keeps operator/maintenance rebuild flows explicit and testable. | If implementation friction appears, keep public retry compatibility and internal rebuild helpers, and narrow the public retry matrix; do not add a public retry-mode parameter in `pr-03` without a roadmap/spec change. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D04 | What is the public retry policy for password-protected/encrypted PDF failures in S6 (`E_PDF_PASSWORD_REQUIRED`) given v1 has no password flow? | Treat password-protected/encrypted PDF failures as terminal for public `POST /media/{id}/retry` in S6 (`409 E_RETRY_NOT_ALLOWED`). No password prompt, credential retry, or hidden redispatch for the same protected file in v1. | Aligns with S6’s explicit no-password-flow constraint and prevents guaranteed-repeat failures/retry churn. Matches the terminal-failure precedent used by EPUB `E_ARCHIVE_UNSAFE`. | If a future slice adds a password flow, revise this policy explicitly in L2/L3 and update `pr-03`-derived behavior by contract change rather than ad hoc exceptions. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D05 | How should `pr-03` derive the PDF capability gate passed to `derive_capabilities(...)` so `GET /media/{id}` and library-list quote/search behavior matches full S6 `pdf_quote_text_ready(media)` semantics given the current seam uses a boolean placeholder named `has_plain_text`? | Introduce an explicit PDF quote-readiness capability seam in `derive_capabilities(...)` (recommended parameter name: `pdf_quote_text_ready`) and compute it in callers from a DB-backed full quote-readiness predicate. Do not use raw plain-text presence alone for PDF quote/search gating. | Aligns code with S6’s actual readiness model, prevents premature quote/search capability enablement on incomplete page-span coverage, and avoids a misleading long-lived parameter name. Preserves the pure-function boundary with a semantically correct input. | If implementation friction appears, keep `derive_capabilities(...)` pure and preserve explicit quote-readiness semantics even if a temporary internal compatibility alias is needed; do not overload `has_plain_text` to mean full quote readiness long term. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D06 | Where should the shared DB-backed PDF quote-readiness predicate helper(s) live so `python/nexus/services/media.py` and `python/nexus/services/libraries.py` can reuse one implementation without circular imports or duplicated SQL? | Use a dedicated `python/nexus/services/pdf_readiness.py` module with shared single-media and batch quote-readiness helpers reused by `media.py` and `libraries.py`, while keeping `python/nexus/services/capabilities.py` pure. | Centralizes a correctness-critical DB-backed predicate in one seam, avoids list/detail SQL drift, preserves `derive_capabilities(...)` purity, and supports batch readiness derivation without N+1 coupling. Keeps layering cleaner than broad service-to-service imports. | If implementation friction appears, preserve one shared readiness seam and batch/list support; do not duplicate quote-readiness SQL in `media.py` and `libraries.py` or move DB access into `capabilities.py`. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D07 | How should `python/nexus/services/pdf_readiness.py` implement `pdf_quote_text_ready(media)` for detail and list surfaces: revalidate full page-span contiguity/coverage on every read, or rely on `pr-03` write-time lifecycle validation with lighter read-time checks? | Enforce full page-span invariants at write time in `pdf_ingest` / `pdf_lifecycle`, and use a shared lightweight DB predicate in `pdf_readiness.py` for detail/list capability gating with fail-closed handling and anomaly logging on impossible states. Do not perform full contiguity revalidation on list reads. | Keeps correctness enforcement where artifacts are authored, avoids expensive/brittle contiguity SQL in list capability reads, and preserves one shared predicate seam for detail/list consistency. Fail-closed behavior protects capability truth if unexpected data drift occurs. | If implementation friction appears, preserve write-time invariant enforcement and one shared read predicate seam; do not duplicate readiness SQL across callers or add heavy read-time contiguity revalidation to list paths. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D08 | What is the `pr-03` lifecycle outcome policy when PDF extraction yields text but `pdf_page_text_spans` lifecycle invariants fail (incomplete page set, non-contiguous offsets, impossible spans): fail extraction or degrade to readable/no-quote? | Treat text-bearing extraction outcomes with page-span lifecycle invariant failures as deterministic extract failures (fail closed), not readable/no-quote degrade paths. Reserve degrade-to-readable for explicit `success_no_text` scanned/image-only outcomes. | Preserves trust in `pdf_quote_text_ready(media)` and the `D07` lightweight read predicate by ensuring invalid indexing artifacts do not silently persist as quote/search inputs. Makes indexing integrity failures explicit, retryable, and operationally visible. | If implementation friction appears, preserve fail-closed extract failure semantics for text-bearing page-span invariant failures; do not silently degrade them to readable/no-quote without an explicit L2/L3 contract change. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D09 | What is the `pr-03` idempotency policy for repeated `POST /media/{id}/ingest` on a non-duplicate PDF after initial dispatch (e.g., already `extracting`, `ready_for_reading`, `failed`): no-op response, redispatch, or conflict? | Make PDF ingest-confirm idempotent and no-redispatch for non-duplicate, non-pending media. Only `pending` may transition to `extracting` and enqueue extraction; other states return the compat response with current `processing_status` and `ingest_enqueued=false` without lifecycle mutation. | Prevents duplicate extraction jobs, makes client retries/timeouts safe, and aligns PDF ingest-confirm behavior with the established EPUB lifecycle orchestration pattern while preserving `IngestResponse` compatibility. | If implementation friction appears, preserve no-redispatch idempotence and compat response shape for non-pending media; do not switch to redispatch-on-repeat or conflict responses without an explicit L2/L3 contract change. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D10 | How should `python/nexus/services/pdf_readiness.py` integrate with `python/nexus/services/libraries.py` for library list capability gating: inline readiness SQL in the main list query, per-row helper calls, or a separate batched readiness query over the paged media IDs? | Use a separate batched readiness query in `pdf_readiness.py` over the already-paged media IDs returned by `libraries.py`, then merge readiness flags in memory before `derive_capabilities(...)`. Avoid per-row DB calls and avoid overloading the main list query with readiness aggregates. | Preserves pagination/order semantics, avoids N+1 list behavior, and keeps readiness SQL centralized in the shared `pdf_readiness.py` seam. Produces a cleaner reusable contract for list/detail callers while respecting `D06/D07`. | If implementation friction appears, preserve one shared batch readiness seam and list/detail consistency; do not duplicate readiness SQL in `libraries.py` or fall back to per-row DB calls. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D11 | What exact public retry inference matrix should `pr-03` use for failed PDF media (`failure_stage` / `last_error_code` -> embedding-only retry vs text-rebuild retry vs terminal disallow) while preserving route compatibility and S6 no-rewrite rules? | Use an explicit precedence-ordered public retry inference matrix: non-`failed` -> `E_RETRY_INVALID_STATE`; `last_error_code=E_PDF_PASSWORD_REQUIRED` -> terminal disallow (`E_RETRY_NOT_ALLOWED`); `failure_stage='embed'` -> embedding/search-only retry path; `failure_stage in {'upload','extract','other'}` -> text-rebuild/extraction retry path; impossible PDF `failure_stage='transcribe'` -> fail closed/internal integrity error. | Makes retry behavior deterministic and testable, preserves terminal password precedence, enforces the S6 no-text-rewrite rule for embed/search failures, and prevents silent coercion of impossible PDF failure-stage states. | If implementation friction appears, preserve the precedence order and the `embed` vs extraction-rebuild split; do not collapse all failed PDF retries into extraction rebuild or silently coerce impossible PDF `failure_stage` values. | Spec owner + platform | pr-03 drafting | Accepted (user-approved 2026-02-25) |
| S6-PR03-D12 | How should `pr-03` classify and persist state when the shared embedding/post-extract handoff fails synchronously after successful PDF extraction (before downstream embedding work runs)? | Treat synchronous embedding handoff/dispatch failures after successful extraction as embed-stage failures (`failure_stage='embed'`) and preserve the successfully persisted PDF extraction artifacts (`media.page_count`, `media.plain_text`, `pdf_page_text_spans`). Do not reclassify as extract failure or roll back extraction artifacts solely because the embedding handoff failed. | Preserves the correctness of the `D11` public retry inference matrix (`failure_stage='embed'` => embedding-only retry), avoids unnecessary extraction rework, and keeps extraction success distinct from embedding dispatch failure for debugging and operations. | If implementation friction appears, preserve embed-stage classification and extracted-artifact preservation for synchronous handoff failures; do not collapse these failures into extract-stage failures without an explicit L2/L3 contract change. | Spec owner + platform | pr-03 drafting | Accepted (audit hardening, 2026-02-25) |

## Notes

- `pr-03` depends on `pr-01` schema/model foundation (`media.plain_text`, `media.page_count`, `pdf_page_text_spans`, PDF quote-match metadata fields on `highlight_pdf_anchors`).
- `S6-PR03-D01` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; `pr-03` uses a dedicated PDF lifecycle/task/domain split and avoids generic file-lifecycle abstraction work.
- `S6-PR03-D02` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; `pr-03` uses PyMuPDF behind parser-agnostic `pdf_ingest` outcomes and keeps parser-specific exception mapping isolated.
- `S6-PR03-D03` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; `pr-03` preserves public retry route compatibility and uses explicit internal rebuild helpers for text-artifact rewrites.
- `S6-PR03-D04` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; password-protected/encrypted PDF failures are terminal for the public retry route in S6 (`E_RETRY_NOT_ALLOWED`).
- `S6-PR03-D05` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; PDF quote/search capability gating now uses an explicit quote-readiness seam (`pdf_quote_text_ready`) rather than overloading raw plain-text presence.
- `S6-PR03-D06` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; `pr-03` uses a dedicated `python/nexus/services/pdf_readiness.py` module for shared DB-backed quote-readiness predicates across media detail and library list capabilities while keeping `derive_capabilities(...)` pure.
- `S6-PR03-D07` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; `pr-03` enforces full page-span invariants at write time and uses a lightweight shared `pdf_readiness.py` predicate (fail-closed + anomaly logging) for detail/list capability gating.
- `S6-PR03-D08` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; text-bearing extraction results that fail `pdf_page_text_spans` lifecycle invariants are deterministic extract failures (fail closed), while degrade-to-readable is reserved for explicit no-text/scanned outcomes.
- `S6-PR03-D09` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; repeated `POST /media/{id}/ingest` for non-duplicate PDF media is idempotent/no-redispatch for non-pending states with compat response shape preserved.
- `S6-PR03-D10` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; library-list capability gating uses a separate batched readiness query in `pdf_readiness.py` over paged media IDs, merged in-memory before `derive_capabilities(...)`.
- `S6-PR03-D11` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; public PDF retry uses a precedence-ordered inference matrix with terminal password disallow, `failure_stage='embed'` embedding-only retry, extraction rebuild for `upload|extract|other`, and fail-closed handling for impossible PDF `failure_stage='transcribe'`.
- `S6-PR03-D12` is resolved and applied in `docs/v1/s6/s6_prs/s6_pr03_spec.md`; synchronous failures in the shared post-extract embedding handoff are classified as embed-stage failures (`failure_stage='embed'`) and preserve successfully persisted extraction artifacts so `D11` retry semantics remain correct.
- Future accepted `pr-03` decisions that affect later PR ownership/constraints must also be reflected in `docs/v1/s6/s6_pr_roadmap.md` carry-forward notes.
