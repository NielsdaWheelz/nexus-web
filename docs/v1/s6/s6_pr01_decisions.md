# Slice 6 PR-01 Decisions

## Decision Ledger

| id | question | decision | rationale | fallback/default | owner | due | status |
|---|---|---|---|---|---|---|---|
| S6-PR01-D01 | What is the pr-01 migration shape for introducing logical highlight anchor fields (`anchor_kind`, `anchor_media_id`) while preserving fragment-only codepaths and tests in a greenfield production baseline? | Use an expand-only migration shape: add typed-anchor foundation tables and logical-highlight fields additively, keep legacy fragment storage as the active compatibility path in `pr-01`, and defer service/route cutover to `pr-02`. | Preserves merge safety and behavior neutrality while establishing the canonical S6 data foundation. Greenfield production removes backfill needs but not code/test compatibility needs. | If implementation friction appears, retain the same expand-only shape and relax dormant-field enforcement (not public behavior) until `pr-02`. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D02 | What exact transitional nullability/default/check contract should `highlights.anchor_kind` and `highlights.anchor_media_id` use in `pr-01` so legacy inserts stay valid while malformed partial writes are minimized? | In `pr-01`, make both fields nullable with no DB defaults, and add DB checks enforcing (a) paired-null semantics and (b) valid `anchor_kind` values when non-NULL. No service writes depend on these fields in `pr-01`. | Reduces malformed dormant state while preserving backward compatibility and avoiding `pr-02` service-cutover leakage. | If a legacy insert path unexpectedly breaks, relax only the paired-null check temporarily (retain nullable/no-default/no-cutover posture) and document the deviation. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D03 | Should `pr-01` populate `highlight_fragment_anchors` for new fragment highlights during the dormant window (e.g., trigger/dual-write), or leave subtype rows absent until `pr-02` adopts typed kernel writes? | Do not populate fragment subtype rows in `pr-01` (no triggers and no service dual-write). `pr-02` owns typed-write adoption plus compatibility normalization/repair for rows created while `pr-01` schema was dormant. | Keeps `pr-01` a pure expand-phase schema/ORM PR and avoids hidden runtime synchronization behavior in a migration PR. | If an unexpected dependency appears before `pr-02`, prefer a narrow repair step in `pr-02` over trigger-based dual-write in `pr-01`. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D04 | How should `pr-01` transition legacy fragment-specific columns on `highlights` (`fragment_id`, `start_offset`, `end_offset`) so future PDF logical highlights can exist in the same table without breaking current fragment APIs/tests? | Use a transitional nullable compatibility bridge in `pr-01`: make fragment columns nullable, replace fragment-only offset checks with conditional fragment-row validity checks, preserve duplicate protection for fragment rows, and allow all-NULL fragment columns for future non-fragment rows. | Unblocks unified logical highlights while preserving current fragment behavior during the dormant window and avoiding a split-core design. | If a legacy path breaks, relax only the new conditional checks/index shape as needed while preserving fragment semantics; do not abandon the unified `highlights` core. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D05 | What exact duplicate-enforcement index strategy should `pr-01` use for fragment rows after `highlights.fragment_id/start_offset/end_offset` become nullable (retain existing unique index vs partial unique index replacement)? | Retain the existing unique index in `pr-01` as the compatibility-first strategy; rely on PostgreSQL NULL-distinct semantics so fragment duplicate protection remains intact for fragment rows while non-fragment rows with NULL legacy fragment columns do not conflict. | Preserves S2 duplicate semantics with minimum migration churn and keeps `pr-01` narrowly scoped. | If typed-kernel rollout exposes a correctness/clarity issue, replace with an explicit partial unique index in a later PR with dedicated tests and roadmap/spec updates. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D06 | What exact `pr-01` schema/index deliverable should satisfy the S6 PDF geometry duplicate-enforcement requirement before `pr-04` adds PDF highlight writes, given `user_id` lives on `highlights` and no PDF writes exist yet? | In `pr-01`, add only supporting PDF-anchor lookup/order indexes; defer exact race-safe PDF duplicate enforcement to `pr-04` when PDF write paths exist (via transactional enforcement and/or additional schema/index strategy if needed). | Keeps `pr-01` a true data-foundation PR and avoids premature denormalization/trigger complexity while remaining consistent with the S6 contract. | If a clean schema-only enforcement path is later justified, add it in a dedicated follow-on with explicit roadmap/L4 updates; `pr-01` remains support-only. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D07 | What DB-level constraints/checks should `pr-01` enforce on `pdf_page_text_spans` versus what must remain a `pr-03` lifecycle/population invariant (e.g., contiguity/full coverage across pages)? | In `pr-01`, enforce row-local validity + uniqueness only (`UNIQUE(media_id,page_number)`, positive page numbers, non-negative offsets, `end_offset >= start_offset`, valid extract-version domain/FK). Defer contiguous coverage/full-page-set/page_count alignment and same-normalization-pass lifecycle guarantees to `pr-03`. | Keeps schema integrity strong enough for dormant data structures without over-scoping `pr-01` into processing/lifecycle logic or triggers. | If a low-risk schema-only constraint is later justified, add it with explicit roadmap/L4 updates; `pr-01` remains row-local-only by default. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D08 | What DB-level `pr-01` checks should exist for `highlight_pdf_quads` row shape (e.g., `quad_idx >= 0`, required coordinate fields) versus geometry normalization/canonicalization behavior deferred to `pr-04`? | In `pr-01`, enforce row-local quad shape integrity only: FK/cascade to `highlights`, ordered-row identity (`highlight_id` + `quad_idx` uniqueness/PK), `quad_idx >= 0`, and required coordinate fields non-null with numeric types. Defer canonicalization, degeneracy rejection, quantization, ordering semantics, and fingerprint correctness to `pr-04`. | Keeps canonical geometry behavior in `pr-04` (where PDF writes exist) while still preventing malformed quad rows at the schema layer. | If a low-risk row-local check is later justified, add it with explicit roadmap/L4 updates; `pr-01` remains row-shape-only by default. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D09 | What DB-level `pr-01` checks should exist on `highlight_pdf_anchors` row-local fields (`page_number`, `rect_count`, match-status/offset coherence`) versus runtime/lifecycle rules owned by `pr-03`/`pr-04`/`pr-05`? | In `pr-01`, enforce row-local shape/domain checks only: 1:1 PK/FK shape, `media_id` FK, `page_number >= 1`, `rect_count >= 1`, `geometry_version >= 1`, match-status enum domain, `plain_text_match_version` null-or-positive domain, non-negative match offsets when present, and paired-null match offsets. Defer page_count range checks, cross-table anchor coherence, `rect_count` vs quad-row count, and match-status/offset semantic coherence to `pr-03`/`pr-04`/`pr-05`. | Keeps schema integrity strong while preserving clean ownership boundaries for lifecycle, write-time, and quote-semantic validation. | If a low-risk row-local check is later justified, add it with explicit roadmap/L4 updates; `pr-01` remains row-local-only by default. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |
| S6-PR01-D10 | What enforcement strategy should later PRs use for cross-table PDF anchor coherence (`highlight_pdf_anchors.media_id == highlights.anchor_media_id`, `highlights.anchor_kind='pdf_page_geometry'`) without introducing fragile trigger logic? | Use a staged hybrid strategy: keep `pr-01` row-local-only; make `pr-04` the authoritative owner of transactional write-time cross-table coherence validation/mismatch rejection for PDF anchors (no triggers); reserve any DB-level hardening for a later dedicated hardening/contraction step only if a clean, low-complexity pattern is justified. | Preserves strong correctness where PDF writes are introduced while avoiding trigger complexity and premature schema hardening in `pr-01`. Maintains clear ownership boundaries and future flexibility. | If a compelling DB-level enforcement pattern emerges later, adopt it in a dedicated follow-on with explicit roadmap/L4 updates; trigger-based enforcement remains out of scope by default. | Spec owner + platform | pr-01 drafting | Accepted (user-approved 2026-02-24) |

## Notes

- Greenfield production assumption removes the need for production data backfill, but does not remove code/test compatibility requirements.
- `pr-01` must remain dormant-safe until `pr-02` adopts typed-highlight kernel behavior.
- `S6-PR01-D01` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; remaining pr-01 drafting should not re-open the expand-only migration shape without explicit review.
- `S6-PR01-D02` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-02` must adopt/canonicalize these dormant fields rather than relying on `pr-01` service writes.
- `S6-PR01-D03` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-02` must handle dormant-window fragment rows without subtype anchors.
- `S6-PR01-D04` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; follow-on PRs should treat fragment legacy columns as a transitional compatibility bridge, not the long-term canonical anchor source.
- `S6-PR01-D05` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; later PRs should preserve fragment duplicate semantics unless a deliberately reviewed partial-index refactor is justified.
- `S6-PR01-D06` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; exact PDF duplicate race-safety remains owned by `pr-04`, while `pr-01` provides supporting indexes only.
- `S6-PR01-D07` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-03` remains owner of contiguous/full-coverage `pdf_page_text_spans` lifecycle enforcement and readiness gating semantics.
- `S6-PR01-D08` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-04` remains owner of PDF geometry canonicalization/degeneracy/order/fingerprint semantics while `pr-01` enforces only quad row-shape integrity.
- `S6-PR01-D09` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-03`/`pr-04`/`pr-05` remain owners of deferred `highlight_pdf_anchors` lifecycle/write-time/quote-semantic validation.
- `S6-PR01-D10` is resolved and applied in `docs/v1/s6/s6_pr01_spec.md`; `pr-04` is the authoritative owner of transactional cross-table PDF anchor coherence enforcement (no triggers), with optional DB hardening deferred to a later dedicated step if justified.
