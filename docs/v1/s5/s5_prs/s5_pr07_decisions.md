# s5 pr-07 decisions

## status
working decision ledger for `docs/v1/s5/s5_prs/s5_pr07.md`.

| id | problem | decision | rejected alternatives | invariant impact | test impact | owner | fallback/default | status |
|---|---|---|---|---|---|---|---|---|
| d-001 | PR-07 acceptance freeze can become a feature PR if implementation gaps are addressed by adding new behavior instead of hardening evidence. | Keep PR-07 hardening-only: add tests/audits and only minimal blocking contract-alignment fixes when a failing invariant requires it. | Implement new feature surfaces to "make tests easier"; defer all hardening to manual QA. | Preserves C8 ownership and prevents scope smuggling across C3-C7. | Requires explicit regression tests for any minimal fix and no speculative behavior changes. | PR-07 implementer | If a gap truly requires new feature work, stop and route to owning slice/PR docs before coding. | locked |
| d-002 | Remaining S5 coverage holes (scenarios 1, 11, 12 and retry cleanup assertions) could be closed with manual audit notes, but that creates weak freeze evidence. | Treat the remaining holes as mandatory automated coverage in PR-07. | Manual verification only; implementation report with no new tests. | Strengthens invariants 6.1, 6.10, and scenario closure reliability. | Adds/extends tests in `python/tests/test_media.py` for immutability, embedding-state transition stability, and retry cleanup/no-mutation assertions. | PR-07 implementer | If a test seam is unavailable, record explicit temporary audit evidence and owner/due before merge. | locked |
| d-003 | L2 scenario 11 requires embedding transition coverage, but no S5-owned backend embedding producer pipeline exists in current codebase. | Verify scenario 11 via contract-state transition tests (controlled status changes) plus read-path readability + fragment immutability assertions; do not implement embedding producer in PR-07. | Build a new embedding job/task in PR-07; omit scenario 11 backend coverage entirely. | Preserves invariant 6.1 and scenario 11 intent without expanding S5 runtime scope. | Requires a backend test that exercises `ready_for_reading -> embedding -> ready` states and asserts stable fragment bytes + readable endpoints. | PR-07 implementer | If a stable merged embedding seam exists later, tests may use it without changing PR-07 scope. | locked |
| d-004 | S5 invariant 6.10 and scenario 12 mention chunk/embedding artifact cleanup, but current EPUB retry cleanup implementation only deletes persisted extraction artifacts in this architecture. | Assert cleanup of all persisted EPUB extraction artifacts (`epub_toc_nodes`, `fragment_blocks`, `fragments`) and document that persisted chunk/embedding tables are not present in current architecture. | Assert cleanup of non-existent tables; weaken the invariant to remove chunk/embedding language. | Keeps invariant 6.10 enforcement architecture-aware while maintaining forward compatibility. | Requires retry cleanup tests to seed and verify deletion of persisted extraction artifacts plus no-mutation behavior on precondition failure. | PR-07 implementer + slice owner | If persisted chunk/embedding tables exist in target branch, extend the cleanup assertions in the same PR. | locked |
| d-005 | Embedding implementation ownership is implicit today, creating risk of indefinite deferral after S5 freeze. | Explicitly assign production embedding pipeline/orchestration and persisted chunk/embedding artifact lifecycle cleanup to S9 (Semantic Search) in roadmap docs and PR-07 boundaries. | Leave ownership implicit in constitution text only; implement partial embedding pipeline in S5 PR-07. | Preserves S5 freeze scope while creating a durable roadmap commitment for future implementation. | No PR-07 runtime tests added solely for this decision; requires roadmap/doc updates and PR-07 boundary text. | Slice owner + PR-07 author | If future planning changes ownership, update top-level roadmap and affected slice docs before implementation. | locked |
| d-006 | PR-07 could duplicate large existing PR-03/04/05/06 coverage suites just to satisfy traceability. | Reuse existing tests as freeze evidence and add only missing hardening tests; strengthen in place rather than clone tests. | Duplicate tests under PR-07-specific names for every scenario. | Maintains traceability while avoiding redundant test noise and maintenance burden. | Traceability matrix and implementation report must cite exact existing test names/files. | PR-07 implementer | If an existing test is too weak, extend it in place with exact assertions. | locked |
| d-007 | PR-07 may uncover L2/L3 contract drift while strengthening tests. | Follow locked process: patch L2/L3 docs first, then revise PR-07 L4 docs before coding further. | Patch implementation first and backfill docs later. | Preserves contract-first SDLC and avoids freeze evidence against stale contracts. | May trigger doc updates before code edits; implementation must pause until docs are aligned. | PR-07 implementer + spec owner | Implementation-local non-contract cleanup may proceed without doc revision. | locked |
| d-008 | Scenario/non-scenario acceptance closure can be hard to review if evidence is only spread across test files and CI logs. | Require a PR-07 implementation report with explicit scenario/non-scenario mapping and invariant/error audit summary. | Rely only on the L4 spec traceability matrix and CI output. | Improves freeze reviewability and long-term auditability without changing runtime behavior. | Adds `docs/v1/s5/s5_prs/s5_pr07_implementation_report.md` as implementation deliverable. | PR-07 implementer | If report generation is deferred during coding, it must be completed before merge approval. | locked |
| d-009 | Legacy EPUB implementation accepted extension-fallback file detection, stored raw/unsanitized concatenated HTML, and lacked durable source preservation, creating concrete regression risks for S5 hardening. | Explicitly include anti-regression traceability/evidence for extension-spoof rejection, active-content sanitization assertions, and retry source-identity preservation in PR-07. | Treat legacy risks as already covered implicitly; rely on broad scenario labels without exact test mapping. | Strengthens freeze confidence for scenario 9 and non-scenario ingest/retry integrity without changing runtime contracts. | Adds exact existing test references (`test_ingest_epub_rejects_extension_only_spoofed_payload`, `test_retry_preserves_source_identity_fields`) and sanitization assertions to PR-07 traceability/audit rerun requirements. | PR-07 author | If a referenced test is missing in target branch, add an equivalent focused regression test before PR-07 freeze sign-off. | locked |
| d-010 | Legacy EPUB spec documents CSS leakage and recommends iframe/shadow DOM/style-stripping isolation options, which could cause scope creep during PR-07 hardening. | Make EPUB CSS isolation/rendering architecture changes an explicit PR-07 non-goal and boundary prohibition. | Fold CSS isolation work into PR-07 as “hardening”; leave the boundary implicit. | Preserves PR-07 hardening-only scope and avoids re-opening reader rendering architecture in freeze PR. | No new runtime tests required; enforce via non-goals/boundaries and review discipline. | PR-07 author | If a blocking security defect requires rendering-architecture changes, stop and re-plan with owning slice docs first. | locked |
| d-011 | PR-07 test additions could drift toward unit-test-heavy coverage that is easier to author but weaker as a slice freeze gate. | Apply an integration-first (testing-trophy) bias for PR-07 hardening: prefer route/service/page integration assertions over new isolated unit tests for contract validation. | Add mostly unit tests around helpers/components for fast coverage counts; rely on manual QA for end-to-end contract proof. | Increases freeze confidence for S5 contract closure by proving behavior through real request/DB/page wiring. | Drives PR-07 deliverables toward `python/tests/test_media.py`, `python/tests/test_epub_ingest.py`, frontend page integration tests, and a real-fixture smoke suite instead of helper-only tests. | PR-07 author | If an integration seam is too unstable to exercise directly, add the smallest justified unit/partial test and document the gap in the implementation report. | locked |
| d-012 | Existing frontend EPUB page tests mock many internal modules/components, which reduces regression signal for reader rendering and attach-flow wiring. | Keep mocking/stubbing limited to externals by default (network/router/runtime APIs) and reduce internal mocks in PR-07 test changes; when unavoidable, use partial mocks that preserve real behavior and document why. | Preserve broad internal mocks for speed/convenience; add more mocked-path assertions only. | Improves confidence in S5 reader/chat compatibility without changing runtime contracts. | Requires strengthening `apps/web/.../media/[id]/page.test.tsx` and conversation route page tests with real component wiring where practical, plus explicit success/failure lifecycle assertions. | PR-07 author | If a full de-mock is too costly for PR-07, reduce the highest-risk mocks first (reader render path / attach success canonicalization) and record remaining debt. | locked |
| d-013 | Synthetic in-memory EPUB builders are strong for deterministic edge cases, but they do not cover parser/fidelity drift from real-world archives. | Retain synthetic builders as the primary edge-case harness and add a small licensed/provenance-documented real EPUB fixture corpus for integration smoke coverage. | Replace synthetic builders entirely with real fixtures; continue with synthetic-only coverage and no real-file smoke suite. | Strengthens scenario 5/8/9/4.8 freeze evidence without broadening S5 runtime scope. | Adds `python/tests/test_epub_ingest_real_fixtures.py` and `python/tests/fixtures/epub/` plus provenance docs, and references them in PR-07 traceability/audit reruns. | PR-07 author + slice owner (fixture provenance review) | If licensing/provenance blocks external fixtures, use locally generated deterministic fixtures and document the temporary limitation in the implementation report. | locked |

## notes
- d-002, d-003, and d-004 were explicitly approved by user as PR-07 hardening strategy decisions.
- d-005 captures the user-approved requirement to make future embedding implementation ownership explicit (S9) instead of leaving it implicit.
- d-006 keeps PR-07 focused on freeze closure quality, not test-suite duplication.
- d-009 and d-010 capture legacy EPUB-spec learnings integrated into PR-07 as anti-regression traceability and scope-guard updates (not feature expansion).
- d-011 through d-013 capture the user-approved PR-07 testing strategy: integration-first coverage, externals-only mocking bias, and a small real EPUB fixture corpus in addition to synthetic builders.
